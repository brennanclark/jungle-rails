require 'rails_helper'

RSpec.describe Product, type: :model do
  describe 'Validations' do
    it "validates the prsence of a name" do

    end
    it "validates the prsence of a price" do

    end
    it "validates the prsence of a quantity" do

    end
    it "validates the prsence of a category" do

    end
  end
end
